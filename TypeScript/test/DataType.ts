import { expect } from 'chai'
import 'mocha'

import { BOOL } from '../src/DataType'

describe('DataType', () => {
  it('can be imported', () => {
    // TODO remove this trivial test, it was used just to setup the test suite.
    expect(BOOL).to.equal(16)
  })
})
